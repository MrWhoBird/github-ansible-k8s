kind: Service                             
apiVersion: v1
metadata:
  name: devopst
  labels:
    app: devopst
spec:
  ports:
    - port: 8085                               
      targetPort: 30                    
  selector:
    app: devopst                    
  type: LoadBalancer                   
